// Declarations of functions which support 
// use-case presented in main.c

// Args.: Input, minChars, maxChars
int validateName(char[]);

// Similarly, 
// int validateDate(char[]);
// int validateEmail(char[]);
// int validatePhone(char[]);

// Add more functions, if required.